<h1>Professores</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Matricula</th>
    <th>Tipo</th>
    <th>Area de conhecimento</th>
    <th>Sub area de conhecimento</th>
    <th>Lattes</th>
	<th></th>
	<th></th>
  </tr>

<% @professores.each do |professor| %>
  <tr class="campo" onclick="window.location = '<%= professor_path(professor) %>'">
    <td><%= professor.nome %></td>
    <td><%= professor.matricula %></td>
    <td><%= professor.tipo %></td>
    <td><%= professor.area_de_conhecimento %></td>
    <td><%= professor.sub_area_de_conhecimento %></td>
    <td><%= professor.lattes %></td>
    <td><%= link_to 'Editar', edit_professor_path(professor) %></td>
    <td><%= link_to 'Deletar', professor, :confirm => 'Você tem certeza?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Professor', new_professor_path %>

