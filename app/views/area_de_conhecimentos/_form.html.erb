<%= form_for(@area_de_conhecimento) do |f| %>
  <% if @area_de_conhecimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@area_de_conhecimento.errors.count, "error") %> prohibited this area_de_conhecimento from being saved:</h2>

      <ul>
      <% @area_de_conhecimento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
