<h1>Listing laboratorios</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Sigla</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @laboratorios.each do |laboratorio| %>
  <tr>
    <td><%= laboratorio.nome %></td>
    <td><%= laboratorio.sigla %></td>
    <td><%= link_to 'Exibir', laboratorio %></td>
    <td><%= link_to 'Editar', edit_laboratorio_path(laboratorio) %></td>
    <td><%= link_to 'Excluir', laboratorio, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Laboratorio', new_laboratorio_path %>
